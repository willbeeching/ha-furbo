# Runtime + test dependencies for the Furbo Bridge add-on's own test suite.
# The add-on image installs aiohttp and cryptography (see Dockerfile); these
# pin the versions the tests run against.
aiohttp==3.14.3
cryptography==50.0.1
pytest==8.3.5
pytest-cov==5.0.0
