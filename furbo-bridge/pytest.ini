[pytest]
# Standalone config so the add-on test run does not inherit the integration's
# pytest settings (asyncio_mode, testpaths) from the repository root.
testpaths = tests
addopts = -p no:cacheprovider
