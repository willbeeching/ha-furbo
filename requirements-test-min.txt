# Minimum supported Home Assistant lane (HA 2025.2.0, Python 3.13).
# PHACC pins Home Assistant and pulls compatible pytest, pytest-cov and aiohttp.
pytest-homeassistant-custom-component==0.13.210

# The camera component imports PyTurboJPEG at module level on newer Home
# Assistant; the test image does not ship it. Pinned to the camera manifest of
# the lane's Home Assistant.
PyTurboJPEG==1.7.5
